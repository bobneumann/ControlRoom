# GUI client
PySide6>=6.6.0
paramiko>=3.4.0
psutil>=5.9.0

# Daemon
fastapi
uvicorn[standard]

# WebSocket client (ws_registry — used by GUI when connecting to daemon)
websockets

# SNMP: no Python package — uses system snmpget binary via subprocess
# Windows (WSL): sudo apt install snmp  (inside WSL)
# Linux native:  sudo apt install snmp
